#ifndef SEED_H
#define  SEED_H
#include <stdexcept>
#include <vector>
#include <algorithm>
#include <random>
#include <chrono>

using namespace std;


extern unsigned seed; // global seed
extern default_random_engine rng; // global rng

#endif
